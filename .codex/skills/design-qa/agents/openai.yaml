interface:
  display_name: "Design QA"
  short_description: "Strict visual-source comparison and iteration gate"
  default_prompt: "Use $design-qa to compare this rendered UI against its visual source and iterate until it passes."
policy:
  allow_implicit_invocation: true
