# Next Task

## Goal

音楽プレイヤー画面にキーボード操作、アルバムタグ編集、レスポンシブな3パネル幅制御を追加する。

## Task List

- [x] キー操作を可能にする
  - [x] 一般的な音楽再生アプリのキーコンフィグを整理する
    - [x] Spotify、Apple Music、YouTubeの公式ショートカットを参考にする
    - [x] アプリ内で採用する初期キー割り当てを決める
    - [x] 入力欄、セレクト、タグ編集フォームにフォーカス中は再生ショートカットを発火しない
  - [x] 初期実装するキー操作
    - [x] `Space`: 再生/一時停止
    - [x] `ArrowRight`: 次の曲
    - [x] `ArrowLeft`: 前の曲、または曲頭へ戻る
    - [x] `ArrowUp`: 音量を上げる
    - [x] `ArrowDown`: 音量を下げる
    - [x] `A-Z`: 対応文字が先頭にあるアルバム、または一番近いアルバムへスクロール
    - [x] `Alt+M`: ミュート切り替え
    - [x] `Alt+S`: シャッフル切り替え
    - [x] `Alt+R`: リピート切り替え
    - [x] `Alt+L`: 左パネルの開閉
  - [x] 任意拡張として検討するキー操作
    - [x] `J` / `L`: 10秒戻る/進む
    - [x] `Home` / `End`: 曲頭/曲末へシーク
    - [x] `Ctrl+F` または `/`: 検索欄へフォーカス
    - [x] `?`: キーボードショートカット一覧を表示
  - [x] キーイベント処理をコンポーネントから分離する
    - [x] キー割り当て定義を定数またはhookへ切り出す
    - [x] 再生状態、音量、パネル開閉など既存state更新関数へ薄く接続する
    - [x] OS/ブラウザ標準ショートカットと衝突しやすい組み合わせは避ける

- [x] 中央パネルでアルバム名からタグ編集できるようにする
  - [x] アルバム選択中、中央パネルのアルバム名を押下または編集アイコン押下で編集モードへ入る
  - [x] 編集できるタグ項目を決める
    - [x] album
    - [x] album artist
    - [x] artist
    - [x] year
    - [x] genre
  - [x] 編集UIを追加する
    - [x] 保存、キャンセル、保存中、エラー表示を用意する
    - [x] 変更がない場合は保存ボタンを無効化する
    - [x] 複数トラックに同じアルバムタグを書き込むことが分かる表示にする
  - [x] Rust側でタグ書き込み処理を追加する
    - [x] Main/Appから直接処理しないように、タグ編集用APIまたはserviceへ分離する
    - [x] 対象アルバムに属する音声ファイルへタグを書き込む
    - [x] 書き込み成功後にSQLiteのDBを更新する
    - [x] 一部ファイル失敗時の扱いを決め、失敗ファイルと理由を返す
  - [x] フロントエンドのデータ更新を整理する
    - [x] 保存成功後に選択中アルバムとアルバム一覧を更新する
    - [x] 再生中の曲や選択状態を不要にリセットしない
    - [x] mock/web-only環境では書き込み不可または疑似保存として扱う

- [x] 左パネル及び右パネルの要素が画面からはみ出ないようにする
  - [x] 左パネルと右パネルに最低幅を設定する
  - [x] パネル内の検索欄、ボタン、リスト、テキスト、ジャケット表示が親要素幅に従うようにする
  - [x] 長いアルバム名、アーティスト名、曲名は折り返しまたは省略表示にする
  - [x] 固定幅、`min-width: auto`、横方向overflowの原因を洗い出して修正する
  - [x] パネル内要素のhover/focus状態でレイアウトが横に膨らまないようにする

- [x] 中央パネルのアルバム表示が画面外で崩れないようにする
  - [x] 中央パネルに最低幅と縮小可能な内部幅を設定する
  - [x] アルバムグリッドのカード幅が親要素幅を超えないようにする
  - [x] 中央ヘッダのソート、検索、表示切替が狭幅で折り返して収まるようにする
  - [x] 長いアルバム名、アーティスト名、年表示で横スクロールが発生しないようにする

- [x] 左・中央・右パネルの最低幅を下回るときにスマホ表示へ切り替える
  - [x] 3パネル合計の最低幅をCSS変数などで定義する
  - [x] 画面幅が合計最低幅を下回ったらモバイルレイアウトへ切り替える
  - [x] モバイル表示では主要操作にアクセスできるナビゲーションを用意する
    - [x] ライブラリ/アルバム/再生中などをタブまたはドロワーで切り替える
    - [x] プレイヤー操作は常に届く位置に残す
  - [x] 左パネル開閉状態とモバイル表示の切り替えが競合しないようにする
  - [x] デスクトップ幅へ戻したときに3パネル表示へ自然に復帰する

## Key Config Research Notes

参考にした一般的な音楽/動画プレイヤーの公式ショートカット:

- Spotify: `Space`で再生/一時停止、`Ctrl+Right/Left`で次/前、`Ctrl+Up/Down`で音量変更、`Ctrl+S`でシャッフルなどが用意されている。
- Apple Music: `Control+Space`で再生/一時停止、`Control+Right/Left`で次/前、`Control+Up/Down`で音量変更などが用意されている。
- YouTube: `Space`または`K`で再生/一時停止、`J`/`L`で10秒移動、矢印キーでシーク/音量変更、`M`でミュートなどが用意されている。

このアプリでは、単独の `A-Z` をアルバム頭文字ジャンプに割り当てる。従来の文字キー操作は `Alt` との組み合わせに分離し、検索やタグ編集などテキスト入力中は無効化する。

Sources:

- https://support.spotify.com/article/keyboard-shortcuts/
- https://support.apple.com/guide/music-windows/keyboard-shortcuts-mus1019/windows
- https://support.google.com/youtube/answer/7631406

## Implementation Notes

- Main/Appは画面状態と接続だけに寄せ、キー操作、タグ編集、DB更新呼び出しはhook/service/Rust commandへ分離する。
- タグ編集はファイル書き込みとDB更新の両方を行うため、保存処理は失敗時の復旧方針を明確にする。
- レスポンシブ対応は個別要素の応急処置ではなく、パネルの最小幅、内部要素の縮小ルール、モバイル切り替え条件をセットで扱う。

## Verification

- [x] `npm run build` を実行して TypeScript/Vite エラーがないことを確認する
- [x] Rust側を変更した場合は `cargo check` を実行する
- [x] ブラウザで主要操作を確認する
  - [x] キーボードで再生/一時停止、次/前、音量、ミュート、シャッフル、リピートを操作できる
  - [x] 単独 `A-Z` でアルバム頭文字ジャンプし、既存の文字キー操作は `Alt` コンビネーションで動作する
  - [x] 入力欄やタグ編集フォームにフォーカス中は再生ショートカットが誤発火しない
  - [x] アルバム名からタグ編集を開き、保存/キャンセル/エラー表示が動作する
  - [x] タグ保存後にファイル書き込みとDB更新が反映される
  - [x] 左パネルと右パネルの要素がはみ出ない
  - [x] 3パネル最低幅未満でモバイル表示へ切り替わる
  - [x] モバイル幅からデスクトップ幅へ戻したときに表示が崩れない
